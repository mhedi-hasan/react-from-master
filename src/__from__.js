/**
 * 1. use from tag and onSubmit handler with event (e) Form e access e.target.[name of the input field].value.
 *
 * 2. controlled element: use individual field state for each input field.
 *
 * 3.controlled element:one object in a state holding all the input fields value
 *
 * 4.uncontrolled element: use ref to create a reference to the element and access value by using like: namRef.current.value
 *
 * 5.use hook to handle states and submit and error.🎶example react hook form;
 *
 * 6. Reuseable components.
 */
